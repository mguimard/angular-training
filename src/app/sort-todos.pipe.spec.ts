import { SortTodosPipe } from './sort-todos.pipe';

describe('SortTodosPipe', () => {
  it('create an instance', () => {
    const pipe = new SortTodosPipe();
    expect(pipe).toBeTruthy();
  });
});
